# Global Claude Rules

Source of truth is `~/.agents/` — do not add rules inline here. Agent-agnostic rules go in
`shared/rules.md`, Claude-only rules in `claude/overlay.md`. See `~/.agents/README.md`.

@~/.agents/shared/rules.md
@~/.agents/claude/overlay.md
